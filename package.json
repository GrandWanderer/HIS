{
  "name": "his-mini-js",
  "version": "0.2.0",
  "private": true,
  "description": "Minimal HIS modules for QA labs (unit tests) - JavaScript (Node.js test runner)",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "coverage": "node --test --experimental-test-coverage"
  }
}
